/**
 * This is a function.
 * @param {*} someArgument - Some data given to the function.
 */
function someFunction(someArgument) {
    console.log("This was given to the function:" + someArgument);
}

// The square brackets are an array
// someFunction([1, 2, "banana"]);
